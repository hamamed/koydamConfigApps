<%- include('partials/head') %>
<div class="ad-shell">
  <%- include('partials/sidebar') %>
  <main class="ad-main">
    <%- include('partials/topbar', {
          heading: 'الفئات',
          subheading: titles.length + ' فئة · ' + used + ' فيها أسئلة — تظهر فوق كل سؤال في التطبيق، وتُعرض في كل صندوق فئة'
        }) %>
    <div class="ad-content">
      <%- include('partials/flash') %>

      <form class="ad-card mb-3" method="post" action="/admin/titles">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="ad-card-head d-flex align-items-center gap-2">
          <i data-lucide="plus" width="16" height="16"></i>
          <span class="fw-semibold">إضافة فئات</span>
        </div>
        <div class="ad-card-body d-flex flex-wrap gap-2 align-items-start">
          <textarea class="form-control wz-ar wz-titles-add" name="names" rows="2" dir="rtl" required
                    placeholder="سيارات&#10;حيوانات أليفة، ماركات ساعات"></textarea>
          <button class="btn btn-kd" type="submit">إضافة</button>
          <div class="form-text w-100">فئة في كل سطر، أو مفصولة بفواصل. حتى 40 حرفاً للواحدة.</div>
        </div>
      </form>

      <div class="ad-card">
        <div class="ad-table-wrap">
          <table class="ad-table">
            <thead><tr><th>الفئة</th><th>الأسئلة</th><th>إعادة تسمية</th><th></th></tr></thead>
            <tbody>
              <% titles.forEach((t) => { %>
                <tr>
                  <td>
                    <span class="kd-tag wz-ar"><%= t.name %></span>
                    <% if (!t.listed) { %><span class="kd-tag kd-tag-warn" title="مستعملة في أسئلة لكنها ليست في القائمة؛ الإضافة أو إعادة التسمية تضيفها">خارج القائمة</span><% } %>
                  </td>
                  <td>
                    <% if (t.questions) { %>
                      <a href="/admin/questions?title=<%= encodeURIComponent(t.name) %>"><%= t.questions %></a>
                    <% } else { %><span class="kd-faint">0</span><% } %>
                  </td>
                  <td>
                    <form class="d-flex gap-1 m-0" method="post" action="/admin/titles/rename">
                      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                      <input type="hidden" name="from" value="<%= t.name %>">
                      <input class="form-control form-control-sm wz-ar wz-title-rename" name="to" value="<%= t.name %>" maxlength="40" dir="rtl"
                             aria-label="اسم جديد لـ <%= t.name %>">
                      <button class="btn btn-sm btn-kd-outline" type="submit">حفظ</button>
                    </form>
                  </td>
                  <td class="text-end text-nowrap">
                    <a class="btn btn-sm btn-kd-ghost" href="/admin/questions/new?title=<%= encodeURIComponent(t.name) %>">
                      <i data-lucide="plus" width="14" height="14" class="me-1"></i>سؤال
                    </a>
                    <% if (!t.questions && t.listed) { %>
                      <form class="d-inline m-0" method="post" action="/admin/titles/delete" data-confirm="حذف «<%= t.name %>» من القائمة؟">
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <input type="hidden" name="name" value="<%= t.name %>">
                        <button class="btn btn-sm btn-kd-ghost" type="submit" aria-label="حذف <%= t.name %>"><i data-lucide="trash-2" width="14" height="14"></i></button>
                      </form>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </main>
</div>
<%- include('partials/foot') %>
