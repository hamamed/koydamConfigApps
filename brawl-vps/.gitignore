node_modules/
.env
.env.local
*.log
npm-debug.log*

# Regenerated by `npm run sync:brawlers` — a stale committed copy is worse than
# no copy, since the server treats an existing file as authoritative until it
# ages out.
data/brawler-meta.json

.DS_Store
.idea/
.vscode/
