<%- include('partials/head', { title }) %>
<div class="sc-login">
  <div class="sc-login-card">
    <div class="text-center mb-4">
      <span class="sc-brand-mark mx-auto mb-3" style="width:46px;height:46px;border-radius:14px;">
        <i data-lucide="shirt" width="24" height="24"></i>
      </span>
      <h1 class="sc-brand-name" style="font-size:1.5rem;">SkinCraft</h1>
      <p class="sc-page-sub">Catalogue administration</p>
    </div>

    <div class="sc-card sc-card-pad">
      <% if (error) { %>
        <div class="alert alert-danger d-flex align-items-center gap-2 mb-3" role="alert">
          <i data-lucide="circle-alert" width="17" height="17"></i>
          <span><%= error %></span>
        </div>
      <% } %>

      <form method="post" action="/admin/login" autocomplete="on">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <input type="hidden" name="next" value="<%= next %>">

        <div class="mb-3">
          <label class="form-label" for="username">Username</label>
          <input class="form-control" id="username" name="username" type="text"
                 autocomplete="username" required autofocus>
        </div>

        <div class="mb-4">
          <label class="form-label" for="password">Password</label>
          <input class="form-control" id="password" name="password" type="password"
                 autocomplete="current-password" required>
        </div>

        <button class="btn btn-sc-primary w-100 py-2" type="submit">
          <i data-lucide="log-in" width="16" height="16" class="me-1"></i>
          Sign in
        </button>
      </form>
    </div>

    <p class="text-center mt-3 mb-0" style="font-size:0.75rem;color:var(--sc-text-dim);">
      Not affiliated with Roblox Corporation.
    </p>
  </div>
</div>
<%- include('partials/foot') %>
