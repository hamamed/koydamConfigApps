<header class="top">
  <strong>Marchés Publics</strong>
  <a href="/admin" class="<%= active === 'dashboard' ? 'active' : '' %>">Tableau de bord</a>
  <a href="/admin/consultations" class="<%= active === 'consultations' ? 'active' : '' %>">Consultations</a>
  <span class="spacer"></span>
  <span class="muted" style="color:#dbe7f5"><%= user.email %></span>
  <form method="post" action="/admin/logout" style="display:inline">
    <button class="secondary" type="submit">Déconnexion</button>
  </form>
</header>
