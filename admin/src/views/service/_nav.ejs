<div class="tabs">
  <a href="/"><%= t('nav.overview') %></a>
  <% config.services.forEach(function (s) { %>
    <a class="<%= s.key === service.key ? 'on' : '' %>" href="/s/<%= s.key %>/dashboard"><%= t(s.label) %></a>
  <% }) %>
</div>

<div class="panel" style="background:transparent; box-shadow:none; padding:0">
  <h1 style="color:#fff; margin-bottom:14px"><%= service.label %></h1>
  <div class="screens screens-dark">
    <% screens.forEach(function (s) { %>
      <a class="<%= s.key === screen ? 'on' : '' %>" href="/s/<%= service.key %>/<%= s.key %>"><%= t(s.label) %></a>
    <% }) %>
    <a href="<%= service.panel %>" target="_blank" rel="noopener noreferrer"><%= t('screen.open') %></a>
  </div>
  <% if (typeof notice !== 'undefined' && notice) { %><div class="notice"><%= t(notice) %></div><% } %>
  <% if (error) { %><div class="down"><%= error %></div><% } %>
